# EReceiptAllInOne — JWT key rotation + Rate Limiting

Run locally:
```
dotnet restore
dotnet run --urls=http://0.0.0.0:8080
```
Docker:
```
docker compose up --build
```
Configure JWT keys and rate limits in `appsettings.json` or env vars.
